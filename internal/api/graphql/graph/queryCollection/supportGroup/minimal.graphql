query ($filter: SupportGroupFilter, $first: Int, $after: String) {
    SupportGroups (
        filter: $filter,
        first: $first,
        after: $after
    ) {
        totalCount
        edges {
            node {
                id
                name
            }
            cursor
        }
    }
}
