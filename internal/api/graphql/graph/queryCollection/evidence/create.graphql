mutation ($input: EvidenceInput!) {
    createEvidence (
        input: $input
    ) {
        id
        description
        raaEnd
        type
        vector
        authorId
        activityId
    }
}