mutation ($id: ID!, $input: ComponentInstanceInput!) {
    updateComponentInstance (
        id: $id,
        input: $input
    ) {
        __typename
        id
        ccrn
        count
        componentVersionId
        serviceId
    }
}